u8 *strcpy(u8 *dest, u8 *src) {
	while *src != 0 {
		*dest = *src;
		dest = dest + 1;
		src = src + 1;
	}
	# copy the null terminator as well
	*dest = *src;
	return dest;
}

u0 main() {
	b8 thing;
	u8 *str = "hello";
	strcpy(&thing, src);
	return 0;
}
